# Hyprland plugins (with hyprpm)
#

plugin {

  hy3 {
    tabs {
      height = 2
      padding = 4
    }
    autotile {
      enable = true
      # # for (7680 x 2160) monitor (4 by 2 scheme)
      #trigger_width = 1900  # split vertically if new window height smaller
      #trigger_height = 900  # split horizontally if new window width smaller
      # for (5120x1440) monitor (4 by 2 scheme)
      trigger_width = 1200
      trigger_height = 700
      # # for x2 (2560 x 1440) monitors  (3 by 2 scheme)
      # trigger_width = 850
      # trigger_height = 600
    }
  }

}
